class Day7 : Day() {

    private data class Input(val i: Int)

    private val transformedInput by lazy {
        Input(1)
    }

    override fun partA() = run {

    }.toString()

    override fun partB() = run {

    }.toString()
}
